<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="JOG &amp; CALIBRATION"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="32dp" />

        <!-- ── Jog Controls (DPad Style) ── -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="DÉPLACEMENT MANUEL (JOG)"
            android:textColor="@color/label_blue"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <RelativeLayout
            android:layout_width="240dp"
            android:layout_height="240dp"
            android:layout_gravity="center"
            android:layout_marginBottom="40dp">

            <!-- Up (MU) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnJogUp"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_centerHorizontal="true"
                android:layout_alignParentTop="true"
                android:text="▲"
                android:textColor="@color/white"
                android:backgroundTint="@color/bg_card"
                app:strokeColor="@color/white"
                app:strokeWidth="1dp"
                app:cornerRadius="12dp" />

            <!-- Left (ML) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnJogLeft"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_centerVertical="true"
                android:layout_alignParentStart="true"
                android:text="◀"
                android:textColor="@color/white"
                android:backgroundTint="@color/bg_card"
                app:strokeColor="@color/white"
                app:strokeWidth="1dp"
                app:cornerRadius="12dp" />

            <!-- Right (MR) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnJogRight"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_centerVertical="true"
                android:layout_alignParentEnd="true"
                android:text="▶"
                android:textColor="@color/white"
                android:backgroundTint="@color/bg_card"
                app:strokeColor="@color/white"
                app:strokeWidth="1dp"
                app:cornerRadius="12dp" />

            <!-- Down (MD) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnJogDown"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_centerHorizontal="true"
                android:layout_alignParentBottom="true"
                android:text="▼"
                android:textColor="@color/white"
                android:backgroundTint="@color/bg_card"
                app:strokeColor="@color/white"
                app:strokeWidth="1dp"
                app:cornerRadius="12dp" />

            <!-- Center Reset (RST) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnReset"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_centerInParent="true"
                android:text="SET\nZERO"
                android:textColor="@color/bg_main"
                android:backgroundTint="@color/white"
                android:textSize="12sp"
                android:textStyle="bold"
                app:cornerRadius="40dp" />

        </RelativeLayout>

        <!-- ── Additional Info ── -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="12dp"
            app:cardBackgroundColor="@color/bg_card"
            app:strokeWidth="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Instructions :"
                    android:textColor="@color/white"
                    android:textStyle="bold"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="1. Utilisez les flèches pour aligner physiquement votre antenne.\n2. Appuyez sur SET ZERO pour définir cette position comme le nouveau 0.0°."
                    android:textColor="@color/text_secondary"
                    android:textSize="13sp" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>
</ScrollView>
