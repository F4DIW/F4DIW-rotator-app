<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.F4DIWRotatorApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="colorPrimaryVariant">@color/grid_blue</item>
        <item name="colorOnPrimary">@color/bg_main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/text_secondary</item>
        <item name="colorSecondaryVariant">@color/accent_blue</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg_main</item>
        <!-- Background -->
        <item name="android:windowBackground">@color/bg_main</item>
        <item name="alertDialogTheme">@style/Theme.F4DIWRotatorApp.Dialog</item>
    </style>

    <style name="Theme.F4DIWRotatorApp.Dialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@color/bg_card</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorAccent">@color/accent_blue</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.F4DIWRotatorApp">
        <!-- We keep this for compatibility but SplashActivity handles the 4s delay -->
    </style>
</resources>
